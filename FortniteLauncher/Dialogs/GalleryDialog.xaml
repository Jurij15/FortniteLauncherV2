<Page
    x:Class="FortniteLauncher.Dialogs.GalleryDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FortniteLauncher.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="900">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid>
            <FlipView x:Name="Gallery" HorizontalAlignment="Stretch"  VerticalAlignment="Center">
            </FlipView>
            <PipsPager x:Name="FlipViewPipsPager"
                        HorizontalAlignment="Center"
                         Margin="0, 12, 0, 0"
                                    NumberOfPages="{x:Bind Gallery.Items.Count}"
                            SelectedPageIndex="{x:Bind Path=Gallery.SelectedIndex, Mode=TwoWay}" />

        </Grid>
    </Grid>
</Page>
